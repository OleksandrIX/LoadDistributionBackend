# Description: Environment variables for the application
# Author: OleksandrIX
# Version: 0.2
# Created: 2024-01-05
# Updated: 2024-05-06

# Before starting, you need to create 4 env files (.env.application, .env.database, .env.logger, .env.minio)

# Application settings
APP_NAME=app-name
APP_VERSION=app-version

# Database settings
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=database_name
DB_USER=database_user
DB_PASSWORD=database_password

# Logging settings
LOG_DIR=log_dir
LOG_LEVEL=log_level
LOG_ROTATION=log_rotation
LOG_COMPRESSION=log_compression

# Minio service settings
MINIO_HOST=minio_host
MINIO_PORT=minio_port
MINIO_ACCESS_KEY=minio_access_key
MINIO_SECRET_KEY=minio_secret_key
MINIO_BUCKET_NAME=minio_bucket_name
MINIO_SECURE=false

# Security settings
ACCESS_TOKEN_EXPIRE=access-token-expire # In minutes
REFRESH_TOKEN_EXPIRE=refresh-token-expire # In minutes
ALGORITHM=algorithm
JWT_SECRET_KEY=jwt-secret-key
JWT_REFRESH_SECRET_KEY=jwt-refresh-secrete-key

# CORS settings
CORS_ALLOW_ORIGINS=<http(s)://127.0.0.1:3000, http(s)://127.0.0.1:3001 | *>
CORS_ALLOW_METHODS=<GET, POST, PUT, DELETE | *>
CORS_ALLOW_HEADERS=<Authorization, Host, User-Agent, etc. | *>
CORS_ALLOW_CREDENTIALS=<true> [if you specify in ALLOWED_ORIGINS, the value is false or missing]
